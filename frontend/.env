# API Configuration
# In development, leave this empty or set to /api to use Vite's proxy (avoids CORS)
# In production, set to your backend URL (e.g., https://your-api.com)
# VITE_API_BASE_URL=http://127.0.0.1:8000
